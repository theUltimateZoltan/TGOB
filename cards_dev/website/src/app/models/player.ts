export class Player {
    email: string
    name: string
    id_jwt: string

    constructor(email: string, name: string, id_jwt: string) {
        this.email = email
        this.name = name   
        this.id_jwt = id_jwt     
    }


}
